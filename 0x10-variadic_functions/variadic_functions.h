#ifndef VARIADIC_FUNCTIONS_H
#define VARIADIC_FUNCTIONS_H

#include <stdarg.h>

int sum_them_all(const unsigned int n, ...);
void print_numbers(const char *separator, const unsigned int n, ...); // Prototype for print_numbers
int _putchar(char c);

#endif /* VARIADIC_FUNCTIONS_H */
